NETHERMIND_VERSION=mainnet-v0.1.15


DATADIR=/nethermind/nethermind_db

# Network Configuration
# We use network_mode host - so the changes here will reflect the ports listening on the host itself
HTTP_ADDR=0.0.0.0
HTTP_PORT=18345
WS_ADDR=0.0.0.0
WS_PORT=18346
MAX_PEERS=32
EXT_IP=
P2P_PORT=25010
METRICS_PORT=8008


# Performance Configuration
MEM_LIMIT=32g