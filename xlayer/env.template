# Before running the project, you must:
# cp env.template .env
# Configure per your needs
# docker compose up -d

# Upstream Docker image tag for hermeznetwork/cdk-erigon
ERIGON_VERSION=latest

# Basic Configuration
DATADIR=/data/erigon-data/xlayer-mainnet

# Network Configuration (host network mode)
HTTP_ADDR=0.0.0.0
HTTP_PORT=8545
WS_ADDR=0.0.0.0
WS_PORT=8546
PRIVATE_API_ADDR=127.0.0.1:9091

# Performance Configuration
MEM_LIMIT=16g

# zkEVM Mainnet essentials (edit these in xlayer.yaml)
# These are documented here for convenience; actual values come from mounted xlayer.yaml
# ZKEVM_L1_RPC_URL=
# ZKEVM_L2_CHAIN_ID=1101
# ZKEVM_L1_CHAIN_ID=1


